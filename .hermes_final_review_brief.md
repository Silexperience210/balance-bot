MISSION : revue finale, en LECTURE SEULE, de tous les changements faits cette nuit dans le firmware BalanceBot. Tu cherches ce qui a été laissé de travers — pas à refaire le travail.

## Dépôt
`/home/silex/balance-bot` (firmware dans `balance-bot/`, ESP32-S3 LilyGo T-Display-S3-Touch).

## Contexte : ce qui a changé cette nuit (commits 871811f, b6a88a3, 741ad5b)
1. **Tactile** : miroir X explicite dans `readTouch()` (l'axe long du CST816 est inversé vs l'écran en rotation 3 — sans lui, appuyer sur AVANT/GAUCHE armait le robot), bornage, rejet des trames poubelles `(4095,4095)`, verrou `g_touchLatch` au changement de mode AUTO↔MANUEL, endpoint `/api/touch` (brut + écran + compteur de taps) + `/api/touch?skip=1` et `?int=1` (diagnostic).
2. **Visage AUTO** : correctifs des 3 bloquants + 4 importants de `FACE_REVIEW.md` (effacement du pourtour en 4 bandes à l'entrée en AUTO, verrou tactile, écran STOP dessiné en 2 images + barre seule, boîte d'effacement des yeux, regard non mirroité + amplitude par expression, hystérésis + maintien 400 ms, batterie/IMU reflétées, aperçu borné à 60 s).
3. **Cadence** : cause racine trouvée = **auto-sleep du CST816** (une lecture pendant son réveil tient le bus I²C ~1000 ms) → correctif `0xFE = 0x01` à l'init du tactile. Documenté dans `STALL_ANALYSIS.md`. Validé : 0 chute en 180 s (21/60 s avant).
4. Instrumentation de cadence ajoutée à `g_state` + exposée par `/api/state` (gap/gph/loopmax/touchmax/drawmax/stalls…).

## Ce que je veux de toi
1. Lis `STALL_ANALYSIS.md`, `FACE_REVIEW.md`, `TOUCH_REVIEW.md`, puis `git log -6 --stat` et `git diff HEAD~3..HEAD` pour voir l'intégralité des changements.
2. **Cherche activement** : régressions, cas limites non traités, incohérences entre correctifs (ex. deux verrous qui se contredisent, un drapeau qui reste armé), code mort, commentaires faux, états impossibles, débordements, courses entre cœurs (le banc web tourne sur le cœur 0, la boucle sur le cœur 1), et tout ce que les trois correctifs de cette nuit auraient pu casser dans le comportement d'origine (écran MANUEL, bouton ÉQUILIBRE, flèches, télémétrie, banc web).
3. **Vérifie par le calcul** les invariants géométriques du visage (symétrie miroir des yeux autour de l'axe x = 159,5, iris contenu dans l'amande pour toutes les expressions et toutes les amplitudes de regard) — tu peux écrire un script Python de contrôle, comme dans `FACE_REVIEW.md`.
4. Compile **sans flasher** avec les flags de `build.sh` (`--warnings all`) et rapporte le résultat exact (flash/RAM, warnings).
5. Vérifie que le tactile peut encore détecter un tap après tous les ajouts (verrou levé au relâchement, trames poubelles rejetées, garde INT désactivée par défaut) — analyse du code, pas de test matériel (c'est moi qui flashe).

## Livrables
- `/home/silex/balance-bot/FINAL_REVIEW.md` : constats numérotés (`fichier:ligne`, gravité, preuve, correctif précis), puis un verdict **« flashable en l'état »** ou **« à corriger »**.
- Réponse finale en **français**, courte : verdict, les défauts retenus par gravité, ce qui a été vérifié et jugé correct.

## Interdictions strictes
- **Ne modifie aucun fichier sauf `FINAL_REVIEW.md`** (pas d'`Edit` sur le code).
- **Ne flashe pas**, ne lance pas `build.sh`, ne lance pas `arduino-cli upload`.
- Ne commite pas, ne pousse pas, ne touche pas à la configuration réseau (pas de `nmcli`), pas de `sudo`, pas d'installation.
